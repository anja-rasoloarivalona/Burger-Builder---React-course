
const aux = (props) => props.children;

export default aux;

//it's just a function which returns the children of the argument props

//We don't have any JSX in this file which is also why we don't have to import React from 'react'